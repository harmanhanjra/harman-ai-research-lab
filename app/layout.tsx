import type { Metadata } from "next";import "./globals.css";export const metadata: Metadata={title:"AI Research Laboratory — Harman Hanjra",description:"Read the experiments, architectures, and evidence behind Harman’s intelligent systems."};export default function RootLayout({children}:Readonly<{children:React.ReactNode}>){return <html lang="en"><body>{children}</body></html>}